import originalAccountsCol from './originalAccountsCol';
import originalReceivableCol from './originalReceivableCol';
import transmissionAccountsCol from './transmissionAccountsCol';
import transmissionReceivableCol from './transmissionReceivableCol';

export default {
    originalAccountsCol,
    originalReceivableCol,
    transmissionAccountsCol,
    transmissionReceivableCol
}